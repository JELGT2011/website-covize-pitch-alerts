<%= form_for(@pitch_event) do |f| %>
  <% if @pitch_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pitch_event.errors.count, "error") %> prohibited this pitch_event from being saved:</h2>

      <ul>
      <% @pitch_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :host %><br>
    <%= f.text_field :host %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
